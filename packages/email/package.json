{
    "name": "@gao/email",
    "version": "0.1.0",
    "description": "GAO Framework — Transactional email with SMTP, templates, and queue integration",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "import": "./dist/index.js",
            "types": "./dist/index.d.ts"
        }
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc --project tsconfig.json",
        "test": "vitest run",
        "test:watch": "vitest watch",
        "typecheck": "tsc --noEmit",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@gao/core": "workspace:*",
        "nodemailer": "^6.9.0"
    },
    "devDependencies": {
        "@types/node": "^25.3.0",
        "@types/nodemailer": "^6.4.0",
        "rimraf": "^6.0.0",
        "typescript": "^5.7.0",
        "vitest": "^3.0.0"
    },
    "peerDependencies": {
        "typescript": ">=5.7.0"
    }
}