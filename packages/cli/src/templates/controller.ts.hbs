import { Controller, Get, Post, Put, Delete, Guard, Validate } from '@gao/http';
import type { GaoRequest, GaoResponse } from '@gao/http';

@Controller('{{routePath}}')
export class {{name}}Controller {

@Get()
async list(req: GaoRequest, res: GaoResponse) {
return res.json({ message: 'List {{name}}' });
}

@Post()
async create(req: GaoRequest, res: GaoResponse) {
return res.json({ message: 'Create {{name}}' });
}

@Get('/:id')
async get(req: GaoRequest, res: GaoResponse) {
const { id } = req.params;
return res.json({ message: `Get {{name}} ${id}` });
}

@Put('/:id')
async update(req: GaoRequest, res: GaoResponse) {
const { id } = req.params;
return res.json({ message: `Update {{name}} ${id}` });
}

@Delete('/:id')
async del(req: GaoRequest, res: GaoResponse) {
const { id } = req.params;
return res.json({ message: `Delete {{name}} ${id}` });
}
}