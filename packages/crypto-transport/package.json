{
    "name": "@gao/crypto-transport",
    "version": "0.1.0",
    "description": "GAO Framework — Transparent End-to-End Encryption: ECDH key exchange, AES-256-GCM transport encryption, auto-encrypt/decrypt middleware",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "import": "./dist/index.js",
            "types": "./dist/index.d.ts"
        }
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc --project tsconfig.json",
        "test": "vitest run",
        "test:watch": "vitest watch",
        "typecheck": "tsc --noEmit",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@gao/core": "workspace:*"
    },
    "devDependencies": {
        "@types/node": "^22.10.1",
        "typescript": "^5.7.0",
        "vitest": "^3.0.0",
        "rimraf": "^6.0.0"
    },
    "peerDependencies": {
        "typescript": ">=5.7.0"
    }
}