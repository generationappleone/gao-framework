{
  "name": "gao-framework",
  "version": "0.1.0",
  "private": true,
  "description": "GAO Framework — Secure, high-performance TypeScript full-stack multi-platform framework",
  "author": "GAO Team",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "biome format --write .",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rimraf node_modules",
    "benchmark": "turbo run benchmark",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "rimraf": "^6.0.0",
    "turbo": "^2.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
